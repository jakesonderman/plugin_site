.DS_Store
node_modules/
.env
*.log

# We'll track all media files with Git LFS now
# Keep the directory structure markers
!downloads/.gitkeep
!img/.gitkeep

# Optional: Add specific files you never want tracked
# downloads/debug-only-*.pkg
# img/temp-*.png 